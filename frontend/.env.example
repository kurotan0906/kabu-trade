# Backend API base URL for production builds (Cloudflare Pages, etc.)
# Leave empty for local development (Vite dev proxy handles /api routing)
# Must be set at BUILD TIME for import.meta.env to pick it up.
# Example: VITE_API_BASE_URL=https://kabu-trade-backend.fly.dev
VITE_API_BASE_URL=
